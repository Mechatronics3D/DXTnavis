<Window x:Class="DXTnavis.Views.DXwindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DXTnavis.Views"
        xmlns:converters="clr-namespace:DXTnavis.Converters"
        mc:Ignorable="d"
        Title="DXTnavis - Navisworks Property Viewer" Height="850" Width="1500"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- 기본 버튼 스타일 -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 초록색 버튼 스타일 -->
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#28A745"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 필터 버튼 스타일 -->
        <Style x:Key="FilterButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#6C757D"/>
            <Setter Property="Padding" Value="8,6"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5A6268"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid 헤더 스타일 -->
        <Style x:Key="DataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <!-- 섹션 타이틀 스타일 -->
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <!-- 레이블 스타일 -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <!-- ═══════════════════════════════════════════════════════════════ -->
        <!-- 왼쪽 패널: Hierarchy Tree -->
        <!-- ═══════════════════════════════════════════════════════════════ -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 헤더 -->
            <StackPanel Grid.Row="0" Margin="0,0,0,8">
                <TextBlock Text="Hierarchy Tree" Style="{StaticResource SectionTitle}"/>
                <TextBlock Text="Level 기반 계층 구조" Style="{StaticResource LabelStyle}"/>

                <!-- 확장/축소 컨트롤 (P1 Enhanced) -->
                <Border Background="#F0F0F0" Padding="6" Margin="0,6,0,0" CornerRadius="3">
                    <StackPanel>
                        <!-- 레벨별 개별 버튼 (P1 Feature) -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBlock Text="Level:" VerticalAlignment="Center" Margin="0,0,6,0" FontSize="11" FontWeight="SemiBold"/>
                            <Button Content="L0" ToolTip="L0까지 확장 (Root)"
                                    Width="28" Height="22" Margin="1,0" FontSize="9" FontWeight="Bold"
                                    Background="#0078D4" Foreground="White" BorderThickness="0"
                                    Command="{Binding ExpandLevelCommand}" CommandParameter="0"/>
                            <Button Content="L1" ToolTip="L1까지 확장"
                                    Width="28" Height="22" Margin="1,0" FontSize="9" FontWeight="Bold"
                                    Background="#28A745" Foreground="White" BorderThickness="0"
                                    Command="{Binding ExpandLevelCommand}" CommandParameter="1"/>
                            <Button Content="L2" ToolTip="L2까지 확장"
                                    Width="28" Height="22" Margin="1,0" FontSize="9" FontWeight="Bold"
                                    Background="#FFC107" Foreground="#333" BorderThickness="0"
                                    Command="{Binding ExpandLevelCommand}" CommandParameter="2"/>
                            <Button Content="L3" ToolTip="L3까지 확장"
                                    Width="28" Height="22" Margin="1,0" FontSize="9" FontWeight="Bold"
                                    Background="#DC3545" Foreground="White" BorderThickness="0"
                                    Command="{Binding ExpandLevelCommand}" CommandParameter="3"/>
                            <Button Content="L4" ToolTip="L4까지 확장"
                                    Width="28" Height="22" Margin="1,0" FontSize="9" FontWeight="Bold"
                                    Background="#6F42C1" Foreground="White" BorderThickness="0"
                                    Command="{Binding ExpandLevelCommand}" CommandParameter="4"/>
                            <Button Content="L5" ToolTip="L5까지 확장"
                                    Width="28" Height="22" Margin="1,0" FontSize="9" FontWeight="Bold"
                                    Background="#20C997" Foreground="White" BorderThickness="0"
                                    Command="{Binding ExpandLevelCommand}" CommandParameter="5"/>
                        </StackPanel>

                        <!-- 기존 컨트롤 (ComboBox + 전체 확장/축소) -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- 레벨 선택 (고급) -->
                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <TextBlock Text="Custom:" VerticalAlignment="Center" Margin="0,0,4,0" FontSize="10" Foreground="#666"/>
                                <ComboBox x:Name="ExpandLevelCombo"
                                          ItemsSource="{Binding AvailableExpandLevels}"
                                          SelectedItem="{Binding SelectedExpandLevel}"
                                          Width="45" Height="22" FontSize="10"/>
                                <Button Content="▶" ToolTip="선택 레벨까지 확장"
                                        Width="22" Height="22" Margin="2,0,0,0" FontSize="10"
                                        Command="{Binding ExpandToLevelCommand}"/>
                            </StackPanel>

                            <!-- 전체 확장/축소 버튼 -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="⊕" ToolTip="전체 확장"
                                        Width="24" Height="22" Margin="4,0,2,0" FontSize="11"
                                        Command="{Binding ExpandAllCommand}"/>
                                <Button Content="⊖" ToolTip="전체 축소"
                                        Width="24" Height="22" Margin="2,0,0,0" FontSize="11"
                                        Command="{Binding CollapseAllCommand}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- TreeView -->
            <Border Grid.Row="1" BorderBrush="#DDDDDD" BorderThickness="1" Background="White">
                <TreeView x:Name="HierarchyTree"
                          ItemsSource="{Binding ObjectHierarchyRoot}"
                          BorderThickness="0"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling">

                    <!-- TreeViewItem 스타일: IsExpanded 바인딩 -->
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                            <Setter Property="Padding" Value="2"/>
                        </Style>
                    </TreeView.ItemContainerStyle>

                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal" Margin="2">
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                          VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <!-- 레벨 표시 배지 -->
                                <Border Background="{Binding LevelColor}" CornerRadius="2" Padding="4,1" Margin="0,0,6,0">
                                    <TextBlock Text="{Binding LevelPrefix}" Foreground="White"
                                               FontFamily="Consolas" FontSize="9" FontWeight="Bold"/>
                                </Border>
                                <!-- 아이콘: 자식 유무에 따라 변경 -->
                                <TextBlock Text="{Binding NodeIcon}" FontSize="12" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                <!-- 이름 -->
                                <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center"/>
                                <!-- 자식 개수 표시 -->
                                <TextBlock Text="{Binding ChildCountText}" Foreground="#999999"
                                           FontSize="10" VerticalAlignment="Center" Margin="6,0,0,0"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Border>

            <!-- 버튼 -->
            <StackPanel Grid.Row="2" Margin="0,8,0,0">
                <Button Content="📂 Load Hierarchy"
                        Style="{StaticResource ActionButton}"
                        Command="{Binding LoadHierarchyCommand}"/>
            </StackPanel>
        </Grid>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="#CCCCCC"/>

        <!-- ═══════════════════════════════════════════════════════════════ -->
        <!-- 중앙 패널: Hierarchy Data + 필터 -->
        <!-- ═══════════════════════════════════════════════════════════════ -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 헤더 -->
            <StackPanel Grid.Row="0" Margin="0,0,0,8">
                <TextBlock Text="Hierarchy Data" Style="{StaticResource SectionTitle}"/>
                <TextBlock Text="속성 데이터 및 필터링" Style="{StaticResource LabelStyle}"/>
            </StackPanel>

            <!-- 필터 영역 (Phase 12: 체크박스 기반) -->
            <Border Grid.Row="1" Background="#E8F4FD" Padding="10" Margin="0,0,0,8" CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Row 0: Level 체크박스 필터 -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="Level:" FontWeight="SemiBold" VerticalAlignment="Center" Width="55"/>
                        <ItemsControl ItemsSource="{Binding LevelFilterOptions}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                              Content="{Binding DisplayText}"
                                              IsEnabled="{Binding IsEnabled}"
                                              ToolTip="{Binding ToolTip}"
                                              Margin="0,0,10,0" FontSize="11"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <!-- Row 1: Category 체크박스 필터 -->
                    <Border Grid.Row="1" Background="#D4EAFC" Padding="4" Margin="0,0,0,6" CornerRadius="2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Category:" FontWeight="SemiBold" VerticalAlignment="Top" Width="55" Margin="0,2,0,0"/>
                            <ScrollViewer MaxHeight="50" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                <ItemsControl ItemsSource="{Binding CategoryFilterOptions}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                                      Content="{Binding DisplayText}"
                                                      IsEnabled="{Binding IsEnabled}"
                                                      ToolTip="{Binding ToolTip}"
                                                      Margin="0,0,8,2" FontSize="10"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>

                    <!-- Row 2: 텍스트 필터 + 버튼 -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- 속성 필터 -->
                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <TextBlock Text="Property Name" Style="{StaticResource LabelStyle}"/>
                            <TextBox Text="{Binding PropertyNameFilter, UpdateSourceTrigger=PropertyChanged}"
                                     Height="26" VerticalContentAlignment="Center"
                                     InputMethod.IsInputMethodEnabled="True"
                                     PreviewKeyDown="TextBox_PreviewKeyDown"
                                     GotFocus="TextBox_GotFocus"/>
                        </StackPanel>

                        <!-- 값 필터 -->
                        <StackPanel Grid.Column="1" Margin="0,0,8,0">
                            <TextBlock Text="Property Value" Style="{StaticResource LabelStyle}"/>
                            <TextBox Text="{Binding PropertyValueFilter, UpdateSourceTrigger=PropertyChanged}"
                                     Height="26" VerticalContentAlignment="Center"
                                     InputMethod.IsInputMethodEnabled="True"
                                     PreviewKeyDown="TextBox_PreviewKeyDown"
                                     GotFocus="TextBox_GotFocus"/>
                        </StackPanel>

                        <!-- 필터 버튼 -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Bottom">
                            <Button Content="🔍 Filter" Style="{StaticResource FilterButton}"
                                    Command="{Binding ApplyFilterCommand}"/>
                            <Button Content="✕ Clear" Style="{StaticResource FilterButton}"
                                    Command="{Binding ClearFilterCommand}"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

            <!-- Object 검색 영역 (v0.4.0) -->
            <Border Grid.Row="2" Background="#FFF8E6" Padding="8" Margin="0,0,0,8" CornerRadius="4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="🔎 Object Search:"
                               VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0"/>

                    <TextBox Grid.Column="1"
                             Text="{Binding ObjectSearchQuery, UpdateSourceTrigger=PropertyChanged}"
                             Height="28" Padding="6,4"
                             VerticalContentAlignment="Center"
                             ToolTip="객체 이름, 속성값, SysPath로 검색 (Enter로 검색)"
                             InputMethod.IsInputMethodEnabled="True"
                             PreviewKeyDown="TextBox_PreviewKeyDown"
                             GotFocus="TextBox_GotFocus"/>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="8,0,0,0">
                        <Button Content="🔍 Search"
                                Style="{StaticResource ActionButton}"
                                Command="{Binding SearchObjectCommand}"
                                ToolTip="입력한 키워드로 객체 검색"
                                Width="80"/>
                        <Button Content="✕ Clear"
                                Style="{StaticResource FilterButton}"
                                Command="{Binding ClearSearchCommand}"
                                ToolTip="검색 결과 초기화"
                                Width="60"/>
                        <Button Content="🔍 Zoom"
                                Style="{StaticResource ActionButton}"
                                Command="{Binding ZoomToSearchResultCommand}"
                                ToolTip="검색 결과 객체로 줌"
                                Width="70"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Select All & View Mode Toggle (Phase 12: 그룹 기반) -->
            <Border Grid.Row="3" Background="#F0F4F8" Padding="8,4" Margin="0,4,0,0" CornerRadius="4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Phase 12: 그룹 Select All (항상 빠름) -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <CheckBox Content="Select All Groups"
                                  IsChecked="{Binding IsGroupSelectAll, Mode=TwoWay}"
                                  VerticalAlignment="Center"
                                  FontWeight="SemiBold"
                                  Foreground="#0078D4"
                                  ToolTip="전체 그룹 선택/해제"/>
                        <TextBlock VerticalAlignment="Center" Margin="8,0,0,0">
                            <Run Text="{Binding SelectionSummary, Mode=OneWay}" Foreground="#666666"/>
                        </TextBlock>
                    </StackPanel>

                    <!-- Grouped/Flat View Toggle -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <CheckBox Content="Flat View"
                                  IsChecked="{Binding IsGroupedViewEnabled, Mode=TwoWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}"
                                  VerticalAlignment="Center"
                                  FontWeight="SemiBold"
                                  ToolTip="플랫 뷰 / 그룹 뷰 전환"
                                  Foreground="#6F42C1"
                                  Visibility="Collapsed"/>
                        <TextBlock VerticalAlignment="Center" Foreground="#6F42C1" FontWeight="SemiBold">
                            <Run Text="{Binding FilteredGroupCount, StringFormat='{}{0:N0} groups', Mode=OneWay}"/>
                            <Run Text=" | " Foreground="#999"/>
                            <Run Text="{Binding FilteredPropertyCount, StringFormat='{}{0:N0} props', Mode=OneWay}"/>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- DataGrid Container (Flat Mode / Grouped Mode 전환) -->
            <Border Grid.Row="4" BorderBrush="#DDDDDD" BorderThickness="1" Background="White">
                <Grid>
                    <!-- Flat Mode DataGrid (기본) -->
                    <DataGrid x:Name="HierarchyDataGrid"
                              ItemsSource="{Binding FilteredHierarchicalProperties}"
                              AutoGenerateColumns="False"
                              SelectionMode="Extended"
                              SelectionUnit="FullRow"
                              AlternatingRowBackground="#FAFAFA"
                              RowBackground="White"
                              ColumnHeaderStyle="{StaticResource DataGridHeaderStyle}"
                              GridLinesVisibility="Horizontal"
                              BorderThickness="0"
                              CanUserAddRows="False"
                              Visibility="{Binding IsGroupedViewEnabled, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="✓"
                                                    Binding="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                    Width="35"/>
                            <DataGridTextColumn Header="Level"
                                                Binding="{Binding Level}"
                                                Width="50" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Object"
                                                Binding="{Binding DisplayName}"
                                                Width="130" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Category"
                                                Binding="{Binding Category}"
                                                Width="100" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Property"
                                                Binding="{Binding PropertyName}"
                                                Width="100" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Raw Value"
                                                Binding="{Binding PropertyValue}"
                                                Width="150" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Refined"
                                                Binding="{Binding RawValue}"
                                                Width="*" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#0078D4"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Unit"
                                                Binding="{Binding Unit}"
                                                Width="60" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Phase 12: Grouped Mode ListView (FilteredObjectGroups 사용) -->
                    <ListView x:Name="GroupedListView"
                              ItemsSource="{Binding FilteredObjectGroups}"
                              VirtualizingStackPanel.IsVirtualizing="True"
                              VirtualizingStackPanel.VirtualizationMode="Recycling"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              BorderThickness="0"
                              Background="White"
                              Visibility="{Binding IsGroupedViewEnabled, Converter={StaticResource BoolToVisibilityConverter}}">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Expander IsExpanded="{Binding IsExpanded, Mode=TwoWay}"
                                          Background="White" BorderThickness="0,0,0,1" BorderBrush="#E0E0E0">
                                    <!-- 그룹 헤더 -->
                                    <Expander.Header>
                                        <Grid Margin="0,4">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <CheckBox Grid.Column="0"
                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,8,0"/>

                                            <Border Grid.Column="1"
                                                    Background="#6F42C1" CornerRadius="2"
                                                    Padding="4,1" Margin="0,0,8,0">
                                                <TextBlock Text="{Binding LevelBadge}"
                                                           Foreground="White" FontSize="10" FontWeight="Bold"/>
                                            </Border>

                                            <TextBlock Grid.Column="2"
                                                       VerticalAlignment="Center">
                                                <Run Text="{Binding DisplayName, Mode=OneWay}" FontWeight="SemiBold" FontSize="13"/>
                                                <Run Text="{Binding FilteredPropertyCount, StringFormat=' ({0} props)', Mode=OneWay}" Foreground="#666"/>
                                            </TextBlock>

                                            <TextBlock Grid.Column="3"
                                                       VerticalAlignment="Center"
                                                       Foreground="#28A745" FontSize="11"
                                                       Margin="8,0"
                                                       Visibility="{Binding IsSelected, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <Run Text="✓ Selected"/>
                                            </TextBlock>
                                        </Grid>
                                    </Expander.Header>

                                    <!-- 필터링된 속성 목록 (확장 시) -->
                                    <DataGrid ItemsSource="{Binding FilteredProperties}"
                                              AutoGenerateColumns="False"
                                              CanUserAddRows="False"
                                              HeadersVisibility="None"
                                              GridLinesVisibility="None"
                                              Background="#FAFAFA"
                                              BorderThickness="0"
                                              Margin="32,0,0,4"
                                              MaxHeight="300">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Binding="{Binding Category}" Width="100" IsReadOnly="True">
                                                <DataGridTextColumn.ElementStyle>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#6F42C1"/>
                                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                                    </Style>
                                                </DataGridTextColumn.ElementStyle>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Binding="{Binding PropertyName}" Width="100" IsReadOnly="True"/>
                                            <DataGridTextColumn Binding="{Binding PropertyValue}" Width="150" IsReadOnly="True"/>
                                            <DataGridTextColumn Binding="{Binding RawValue}" Width="*" IsReadOnly="True">
                                                <DataGridTextColumn.ElementStyle>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#0078D4"/>
                                                    </Style>
                                                </DataGridTextColumn.ElementStyle>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Binding="{Binding Unit}" Width="50" IsReadOnly="True">
                                                <DataGridTextColumn.ElementStyle>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#999"/>
                                                    </Style>
                                                </DataGridTextColumn.ElementStyle>
                                            </DataGridTextColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Expander>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>

            <!-- 내보내기 및 3D 선택 버튼 -->
            <Border Grid.Row="5" Background="#F8F9FA" Padding="8" Margin="0,8,0,0" CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Main Export Buttons -->
                    <StackPanel Grid.Row="0" Grid.Column="0">
                        <!-- Export Progress -->
                        <StackPanel Visibility="{Binding IsExporting, Converter={StaticResource BoolToVisibilityConverter}}"
                                    Margin="0,0,0,4">
                            <TextBlock Text="{Binding ExportStatusMessage}"
                                       FontSize="11" Foreground="#1565C0" TextWrapping="Wrap"/>
                            <ProgressBar Value="{Binding ExportProgressPercentage}"
                                         Minimum="0" Maximum="100" Height="8" Margin="0,2,0,0"/>
                            <TextBlock Text="{Binding ExportGlbProgressText}"
                                       FontSize="10" Foreground="#1976D2" FontWeight="Bold"
                                       Margin="0,2,0,0"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Button Content="📦 Export"
                                    Command="{Binding ExportFullPipelineCommand}"
                                    ToolTip="선택 객체 통합 출력: AllProperties + Unified CSV + Geometry + Mesh GLB + Adjacency"
                                    Width="130" FontWeight="Bold">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource ActionButton}">
                                        <Setter Property="Background" Value="#FF4500"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Background" Value="#CCCCCC"/>
                                                <Setter Property="Foreground" Value="#888888"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button Content="📊 AllProperties"
                                    Style="{StaticResource ActionButton}"
                                    Command="{Binding ExportAllPropertiesCommand}"
                                    ToolTip="전체 모델의 모든 속성을 CSV로 내보내기 (Pipeline 4D 입력용)"
                                    Width="120" Margin="4,0,0,0"/>
                            <Button Content="📊 Refined XLSX"
                                    Style="{StaticResource ActionButton}"
                                    Background="#217346" Foreground="White"
                                    Command="{Binding ExportRefinedXlsxCommand}"
                                    ToolTip="Refining_ObjectID 형식의 서식 XLSX 내보내기 (5개 시트, 색상 코딩)"
                                    Width="120" Margin="4,0,0,0"/>
                        </StackPanel>
                        <!-- Advanced Export (collapsed by default) -->
                        <Expander Header="▶ Advanced Export" IsExpanded="False" Margin="0,4,0,0" FontSize="10">
                            <StackPanel Margin="4,4,0,0">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,3">
                                    <TextBlock Text="Selection:" Width="55" FontSize="10" VerticalAlignment="Center"/>
                                    <Button Content="Properties" Style="{StaticResource FilterButton}"
                                            Command="{Binding ExportSelectionPropertiesCommand}" Width="70" FontSize="10" Padding="4,3"/>
                                    <Button Content="Hierarchy" Style="{StaticResource FilterButton}"
                                            Command="{Binding ExportSelectionHierarchyCommand}" Width="65" FontSize="10" Padding="4,3"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,3">
                                    <TextBlock Text="Geometry:" Width="55" FontSize="10" VerticalAlignment="Center"/>
                                    <Button Content="All BBox" Style="{StaticResource FilterButton}"
                                            Command="{Binding ExportGeometryCommand}" Width="70" FontSize="10" Padding="4,3" Background="#9C27B0"/>
                                    <Button Content="Selection" Style="{StaticResource FilterButton}"
                                            Command="{Binding ExportSelectionGeometryCommand}" Width="65" FontSize="10" Padding="4,3" Background="#7B1FA2"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,3">
                                    <TextBlock Text="Spatial:" Width="55" FontSize="10" VerticalAlignment="Center"/>
                                    <Button Content="Adjacency" Style="{StaticResource FilterButton}"
                                            Command="{Binding ExportAdjacencyCommand}" Width="70" FontSize="10" Padding="4,3" Background="#00695C"/>
                                    <Button Content="Selection" Style="{StaticResource FilterButton}"
                                            Command="{Binding ExportSelectionAdjacencyCommand}" Width="65" FontSize="10" Padding="4,3" Background="#004D40"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Debug:" Width="55" FontSize="10" VerticalAlignment="Center"/>
                                    <Button Content="Test Mesh" Style="{StaticResource FilterButton}"
                                            Command="{Binding ExportTestMeshCommand}" Width="70" FontSize="10" Padding="4,3" Background="#6A1B9A"/>
                                    <Button Content="Diag" Style="{StaticResource FilterButton}"
                                            Command="{Binding ExportMeshDiagnosticCommand}" Width="50" FontSize="10" Padding="4,3" Background="#BF360C"/>
                                </StackPanel>
                            </StackPanel>
                        </Expander>
                    </StackPanel>

                    <!-- 3D 선택 버튼 (Phase 3) -->
                    <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Horizontal">
                        <Button Content="🎯 Select in 3D"
                                Style="{StaticResource ActionButton}"
                                Command="{Binding SelectIn3DCommand}"
                                ToolTip="필터링된 객체를 Navisworks 3D 뷰에서 선택"/>
                        <!-- v0.4.3: Show Only 토글 버튼 -->
                        <Button Content="{Binding ShowOnlyButtonText}"
                                Command="{Binding ShowOnlyFilteredCommand}"
                                ToolTip="Show Only 토글 (On: 필터링 객체만 표시 / Off: 전체 표시)"
                                Padding="12,8" Margin="3" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand"
                                Foreground="White">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="#0078D4"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsShowOnlyActive}" Value="True">
                                            <Setter Property="Background" Value="#FF5722"/>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#005A9E"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="🔍 Zoom"
                                Style="{StaticResource ActionButton}"
                                Command="{Binding ZoomToFilteredCommand}"
                                ToolTip="필터링된 객체로 카메라 줌"/>
                    </StackPanel>

                    <!-- 스냅샷 버튼 (Phase 4) -->
                    <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="0,6,0,0">
                        <TextBlock Text="📸 Snapshot:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
                        <Button Content="📷 Capture View"
                                Style="{StaticResource ActionButton}"
                                Command="{Binding CaptureViewCommand}"
                                ToolTip="현재 3D 뷰를 이미지로 캡처"/>
                        <Button Content="📍 Save ViewPoint"
                                Style="{StaticResource ActionButton}"
                                Command="{Binding SaveViewPointCommand}"
                                ToolTip="현재 뷰포인트를 저장"/>
                        <Button Content="📸 Capture + ViewPoint"
                                Style="{StaticResource SuccessButton}"
                                Command="{Binding CaptureWithViewPointCommand}"
                                ToolTip="이미지 캡처와 뷰포인트를 함께 저장"/>
                        <Button Content="🎬 Batch Capture"
                                Style="{StaticResource ActionButton}"
                                Command="{Binding BatchCaptureCommand}"
                                ToolTip="필터링된 객체들을 개별 캡처"/>
                        <Button Content="🏠 Reset Home"
                                Style="{StaticResource FilterButton}"
                                Command="{Binding ResetToHomeCommand}"
                                ToolTip="관측점을 초기 상태로 리셋 (Home 뷰포인트 또는 Zoom Extents)"/>
                    </StackPanel>

                    <!-- 상태 메시지 -->
                    <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3"
                               Text="{Binding StatusMessage}"
                               VerticalAlignment="Center" Margin="0,6,0,0"
                               FontSize="11" Foreground="#666666"/>
                </Grid>
            </Border>
        </Grid>

        <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Stretch" Background="#CCCCCC"/>

        <!-- ═══════════════════════════════════════════════════════════════ -->
        <!-- 오른쪽 패널: Filter & Search Set -->
        <!-- ═══════════════════════════════════════════════════════════════ -->
        <TabControl Grid.Column="4" Background="White">

            <!-- 탭 1: Pipeline 4D -->
            <TabItem Header="Pipeline 4D" IsSelected="True">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="12">
                        <TextBlock Text="Pipeline 4D Schedule" FontSize="16" FontWeight="Bold"
                                   Foreground="#0078D4" Margin="0,0,0,12"/>

                        <!-- 데이터 소스 -->
                        <Border Background="#F8F9FA" Padding="12" Margin="0,0,0,12" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="데이터 소스" FontWeight="Bold" Margin="0,0,0,10"/>

                                <!-- AllProperties CSV -->
                                <TextBlock Text="AllProperties CSV (필수)" FontSize="11" Foreground="#666" Margin="0,0,0,4"/>
                                <DockPanel Margin="0,0,0,8">
                                    <Button Content="찾아보기" DockPanel.Dock="Right"
                                            Command="{Binding Pipeline4D.BrowseCsvCommand}"
                                            Width="70" Height="26" Margin="6,0,0,0"/>
                                    <TextBox Text="{Binding Pipeline4D.CsvPath, FallbackValue=''}" IsReadOnly="True"
                                             Height="26" Padding="6,4" Background="#F0F0F0"/>
                                </DockPanel>

                                <!-- Geometry CSV (선택) -->
                                <TextBlock Text="Geometry CSV (선택, 공간정렬용)" FontSize="11" Foreground="#666" Margin="0,0,0,4"/>
                                <DockPanel Margin="0,0,0,4">
                                    <Button Content="찾아보기" DockPanel.Dock="Right"
                                            Command="{Binding Pipeline4D.BrowseGeometryCsvCommand}"
                                            Width="70" Height="26" Margin="6,0,0,0"/>
                                    <TextBox Text="{Binding Pipeline4D.GeometryCsvPath, FallbackValue=''}" IsReadOnly="True"
                                             Height="26" Padding="6,4" Background="#F0F0F0"/>
                                </DockPanel>
                            </StackPanel>
                        </Border>

                        <!-- 시간 매핑 옵션 -->
                        <Border Background="#F8F9FA" Padding="12" Margin="0,0,0,12" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="시간 매핑" FontWeight="Bold" Margin="0,0,0,10"/>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="32"/>
                                        <RowDefinition Height="32"/>
                                        <RowDefinition Height="32"/>
                                        <RowDefinition Height="32"/>
                                        <RowDefinition Height="32"/>
                                        <RowDefinition Height="32"/>
                                        <RowDefinition Height="32"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Text="시작일" VerticalAlignment="Center" Grid.Row="0"/>
                                    <DatePicker SelectedDate="{Binding Pipeline4D.StartDate}" Height="26" Grid.Row="0" Grid.Column="1"/>

                                    <TextBlock Text="기본 시간(h)" VerticalAlignment="Center" Grid.Row="1"/>
                                    <TextBox Text="{Binding Pipeline4D.BaseDuration, UpdateSourceTrigger=PropertyChanged}"
                                             Height="26" Padding="6,4" Grid.Row="1" Grid.Column="1"
                                             PreviewKeyDown="TextBox_PreviewKeyDown" GotFocus="TextBox_GotFocus"/>

                                    <TextBlock Text="객체당 추가(h)" VerticalAlignment="Center" Grid.Row="2"/>
                                    <TextBox Text="{Binding Pipeline4D.PerObjectHours, UpdateSourceTrigger=PropertyChanged}"
                                             Height="26" Padding="6,4" Grid.Row="2" Grid.Column="1"
                                             PreviewKeyDown="TextBox_PreviewKeyDown" GotFocus="TextBox_GotFocus"/>

                                    <TextBlock Text="Pipeline 간격(일)" VerticalAlignment="Center" Grid.Row="3"/>
                                    <TextBox Text="{Binding Pipeline4D.GapDays, UpdateSourceTrigger=PropertyChanged}"
                                             Height="26" Padding="6,4" Grid.Row="3" Grid.Column="1"
                                             PreviewKeyDown="TextBox_PreviewKeyDown" GotFocus="TextBox_GotFocus"/>

                                    <TextBlock Text="시간 전략" VerticalAlignment="Center" Grid.Row="4"/>
                                    <ComboBox ItemsSource="{Binding Pipeline4D.TimeStrategies}"
                                              SelectedItem="{Binding Pipeline4D.SelectedTimeStrategy}"
                                              Height="26" Grid.Row="4" Grid.Column="1"/>

                                    <TextBlock Text="정렬 전략" VerticalAlignment="Center" Grid.Row="5"/>
                                    <ComboBox ItemsSource="{Binding Pipeline4D.OrderingStrategies}"
                                              SelectedItem="{Binding Pipeline4D.SelectedOrderingStrategy}"
                                              Height="26" Grid.Row="5" Grid.Column="1"/>

                                    <TextBlock Text="Task 단위:" VerticalAlignment="Center" Grid.Row="6" FontSize="11"/>
                                    <StackPanel Orientation="Horizontal" Grid.Row="6" Grid.Column="1" VerticalAlignment="Center">
                                        <RadioButton Content="PipeRun별" GroupName="TaskGranularity"
                                                     IsChecked="{Binding Pipeline4D.IsByPipeRun}" Margin="0,0,12,0"/>
                                        <RadioButton Content="객체별" GroupName="TaskGranularity"
                                                     IsChecked="{Binding Pipeline4D.IsByObject}"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- 미리보기 DataGrid -->
                        <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1"
                                Padding="1" CornerRadius="4" MaxHeight="250" Margin="0,0,0,12">
                            <DataGrid ItemsSource="{Binding Pipeline4D.PreviewItems}"
                                      AutoGenerateColumns="False"
                                      IsReadOnly="True"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      RowHeight="24"
                                      HeadersVisibility="Column"
                                      GridLinesVisibility="Horizontal"
                                      HorizontalGridLinesBrush="#EEEEEE"
                                      AlternatingRowBackground="#FAFAFA"
                                      EnableRowVirtualization="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Pipeline" Binding="{Binding Pipeline}" Width="80"/>
                                    <DataGridTextColumn Header="PipeRun" Binding="{Binding PipeRun}" Width="*"/>
                                    <DataGridTextColumn Header="객체명" Binding="{Binding ObjectName}" Width="120"/>
                                    <DataGridTextColumn Header="Objects" Binding="{Binding ObjectCount}" Width="55"/>
                                    <DataGridTextColumn Header="Start" Binding="{Binding Start, StringFormat='yyyy-MM-dd'}" Width="80"/>
                                    <DataGridTextColumn Header="End" Binding="{Binding End, StringFormat='yyyy-MM-dd'}" Width="80"/>
                                    <DataGridTextColumn Header="Days" Binding="{Binding DurationDays}" Width="40"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>

                        <!-- 실행 버튼 -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8">
                            <CheckBox Content="DryRun" IsChecked="{Binding Pipeline4D.IsDryRun}"
                                      VerticalAlignment="Center" Margin="0,0,12,0"
                                      ToolTip="체크하면 실제 실행 없이 결과를 미리 확인합니다"/>
                            <Button Content="미리보기"
                                    Style="{StaticResource FilterButton}"
                                    Command="{Binding Pipeline4D.PreviewCommand}"
                                    Width="80" Height="28" Margin="0,0,4,0"
                                    ToolTip="CSV 파싱 및 스케줄 미리보기"/>
                            <Button Content="CSV 내보내기"
                                    Style="{StaticResource SuccessButton}"
                                    Command="{Binding Pipeline4D.ExportCsvCommand}"
                                    Width="90" Height="28" Margin="0,0,4,0"
                                    ToolTip="Navisworks TimeLiner Import용 CSV 파일 내보내기"/>
                            <Button Content="TimeLiner 실행"
                                    Background="#0078D4" Foreground="White"
                                    Command="{Binding Pipeline4D.ExecuteCommand}"
                                    Width="100" Height="28"
                                    ToolTip="Selection Set + TimeLiner Task 자동 생성"/>
                        </StackPanel>

                        <!-- 진행률 -->
                        <Grid Visibility="{Binding Pipeline4D.IsExecuting, Converter={StaticResource BoolToVisibilityConverter}}"
                              Margin="0,0,0,8">
                            <ProgressBar Value="{Binding Pipeline4D.Progress}"
                                         Minimum="0" Maximum="100"
                                         Height="6" Background="#E3F2FD" Foreground="#0078D4"/>
                        </Grid>

                        <!-- 상태 메시지 -->
                        <TextBlock Text="{Binding Pipeline4D.StatusMessage}" Foreground="Gray" FontSize="11"
                                   TextWrapping="Wrap" Margin="0,0,0,8"/>

                        <!-- 사용법 안내 -->
                        <Border Background="#F0F0F0" Padding="10" Margin="0,4,0,0" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="Pipeline 4D 사용법" FontWeight="Bold" Margin="0,0,0,6"/>
                                <TextBlock Text="1. AllProperties CSV 파일을 선택 (Pipeline/PipeRun 컬럼 필요)" FontSize="10" Foreground="#666"/>
                                <TextBlock Text="2. 시간 매핑 파라미터 설정 (시작일, 기본시간, 객체당 추가시간)" FontSize="10" Foreground="#666"/>
                                <TextBlock Text="3. '미리보기'로 스케줄 확인" FontSize="10" Foreground="#666"/>
                                <TextBlock Text="4. 'TimeLiner 실행'으로 Selection Set + Task 자동 생성" FontSize="10" Foreground="#666"/>
                                <TextBlock Text="   또는 'CSV 내보내기'로 Navisworks Import용 파일 생성" FontSize="10" Foreground="#666"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- 탭 2: Search Set 생성 -->
            <TabItem Header="🔍 Search Set">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="12">
                        <TextBlock Text="Search Set 생성" Style="{StaticResource SectionTitle}"/>
                        <TextBlock Text="필터링된 데이터로 Navisworks 검색 세트 생성"
                                   Style="{StaticResource LabelStyle}" Margin="0,0,0,15"/>

                        <!-- 선택 상태 -->
                        <Border Background="#E6F3FF" BorderBrush="#0078D4" BorderThickness="1"
                                Padding="12" Margin="0,0,0,15" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="선택된 항목" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding SelectedPropertiesCount, FallbackValue='0개 선택됨'}"
                                           FontSize="18" FontWeight="Bold" Foreground="#0078D4"/>
                            </StackPanel>
                        </Border>

                        <!-- 폴더 이름 -->
                        <TextBlock Text="폴더 이름" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding FolderName, UpdateSourceTrigger=PropertyChanged}"
                                 Height="30" Margin="0,0,0,12" Padding="8,5"
                                 InputMethod.IsInputMethodEnabled="True"
                                 PreviewKeyDown="TextBox_PreviewKeyDown"
                                 GotFocus="TextBox_GotFocus"/>

                        <!-- 세트 이름 -->
                        <TextBlock Text="세트 이름" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding NewSetName, UpdateSourceTrigger=PropertyChanged}"
                                 Height="30" Margin="0,0,0,15" Padding="8,5"
                                 InputMethod.IsInputMethodEnabled="True"
                                 PreviewKeyDown="TextBox_PreviewKeyDown"
                                 GotFocus="TextBox_GotFocus"/>

                        <!-- 선택된 속성 미리보기 -->
                        <TextBlock Text="선택된 속성 미리보기" Style="{StaticResource LabelStyle}"/>
                        <Border Background="#F8F9FA" BorderBrush="#DDDDDD" BorderThickness="1"
                                Padding="10" Margin="0,0,0,15" MaxHeight="200">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <TextBlock Text="{Binding SelectedPropertyInfo, FallbackValue='(Hierarchy Data에서 항목을 선택하세요)'}"
                                           TextWrapping="Wrap" FontSize="11" Foreground="#666666"/>
                            </ScrollViewer>
                        </Border>

                        <!-- 생성 버튼 -->
                        <Button Content="🔍 Search Set 생성"
                                Style="{StaticResource SuccessButton}"
                                HorizontalAlignment="Stretch"
                                Height="40"
                                Command="{Binding CreateSearchSetCommand}"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- 탭 3: 정보 -->
            <TabItem Header="ℹ️ Info">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="12">
                        <TextBlock Text="DXTnavis" FontSize="22" FontWeight="Bold"
                                   Foreground="#0078D4" Margin="0,0,0,5"/>
                        <TextBlock Text="Navisworks 2025 Property Viewer"
                                   FontSize="12" Foreground="#666666" Margin="0,0,0,20"/>

                        <Border Background="#F8F9FA" Padding="15" Margin="0,0,0,15" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="주요 기능" FontWeight="Bold" FontSize="13" Margin="0,0,0,12"/>

                                <TextBlock Text="• CSV Export (4종 버튼)" FontWeight="SemiBold" Margin="0,0,0,3"/>
                                <TextBlock Text="  All/Selection × Properties/Hierarchy 조합"
                                           FontSize="10" Foreground="#666666" Margin="0,0,0,10"/>

                                <TextBlock Text="• Level 기반 트리 제어" FontWeight="SemiBold" Margin="0,0,0,3"/>
                                <TextBlock Text="  L0~L5 버튼으로 트리 레벨별 확장/축소"
                                           FontSize="10" Foreground="#666666" Margin="0,0,0,10"/>

                                <TextBlock Text="• Category/Property 필터" FontWeight="SemiBold" Margin="0,0,0,3"/>
                                <TextBlock Text="  카테고리, 속성명, 값으로 데이터 필터링"
                                           FontSize="10" Foreground="#666666" Margin="0,0,0,10"/>

                                <TextBlock Text="• 3D 선택 및 뷰포인트" FontWeight="SemiBold" Margin="0,0,0,3"/>
                                <TextBlock Text="  Select in 3D, Show Only, Zoom, ViewPoint 저장"
                                           FontSize="10" Foreground="#666666" Margin="0,0,0,10"/>

                                <TextBlock Text="• AWP 4D Automation" FontWeight="SemiBold" Margin="0,0,0,3"/>
                                <TextBlock Text="  CSV → Property → Selection Set → TimeLiner 자동화"
                                           FontSize="10" Foreground="#666666" Margin="0,0,0,10"/>

                                <TextBlock Text="• Schedule Builder (NEW)" FontWeight="SemiBold" Margin="0,0,0,3"/>
                                <TextBlock Text="  선택 데이터 → Schedule CSV 자동 생성"
                                           FontSize="10" Foreground="#666666"/>
                            </StackPanel>
                        </Border>

                        <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1"
                                Padding="15" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="버전 정보" FontWeight="Bold" Margin="0,0,0,10"/>
                                <TextBlock Text="Version: 0.8.0" FontSize="11" Margin="0,0,0,3"/>
                                <TextBlock Text="Target: Navisworks Manage 2025" FontSize="11" Margin="0,0,0,3"/>
                                <TextBlock Text="Framework: .NET Framework 4.8" FontSize="11" Margin="0,0,0,3"/>
                                <TextBlock Text="GitHub: github.com/tygwan/DXTnavis"
                                           FontSize="11" Foreground="#0078D4"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
